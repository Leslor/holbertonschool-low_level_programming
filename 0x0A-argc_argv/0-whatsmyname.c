#include <stdio.h>

/**
 * main -  main funtions
 * @argc: argument count
 * @argv: pointer to a string
 * Return: Return 0 value
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
